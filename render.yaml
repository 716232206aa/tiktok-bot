Enterservices:
    - type: worker
    name: tiktok-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python tiktok_bot.py
    envVars:
            - key: BOT_TOKEN
        sync: false
